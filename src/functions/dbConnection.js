import axios from 'axios';

const databaseAPI = 'http://localhost:8010/database';

//create a unique source to connect to database

let database;
//const getData = () => axios.get(databaseAPI).then(res => {   
    // console.log("db connection", res.data);
//});
//getData();

//CRUD fuctions to export.
//console.log(database)
export default database;